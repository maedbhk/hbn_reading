from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Analysis of phenotypic data (e.g., clinical questionnaires) for children with reading impairments, recruited via healthy brain network',
    author='Maedbh King',
    license='MIT',
)
